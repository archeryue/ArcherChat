# Test Environment Configuration
# Copy this file to .env.test and fill in the values
# IMPORTANT: .env.test is gitignored and should NEVER be committed

# Node environment (must be 'test' for test auth to work)
NODE_ENV=test

# Enable test authentication provider (ONLY works on localhost)
# SECURITY: This will NOT work on any deployed environment
ENABLE_TEST_AUTH=true

# Test user credentials (used by test auth provider)
# These can be any values - they're only for automated testing
TEST_USER_ID=test-user-123
TEST_USER_EMAIL=test@example.com
TEST_USER_NAME=Test User

# IMPORTANT: Add this email to Firestore whitelist collection
# Otherwise the test user won't be able to access the app
# Run: firebase firestore:add whitelist test@example.com

# Copy all other environment variables from .env.local
# (Gemini API key, Firebase config, etc.)
